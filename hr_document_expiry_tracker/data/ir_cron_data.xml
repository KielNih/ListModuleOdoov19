<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="cron_hr_document_expiry_tracker" model="ir.cron">
            <field name="name">HR: Track Document Expiry</field>
            <field name="model_id" ref="model_hr_employee_document"/>
            <field name="state">code</field>
            <field name="code">model._cron_check_document_expiry()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=8, minute=0, second=0).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>
    </data>
</odoo>